naftiko: 1.0.0-alpha2
info:
  label: github-repos-api — Repositories
  description: 'github-repos-api — Repositories. 3 operations. Lead operation: GitHub Get Repository Subscription. Self-contained
    Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Repositories
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-subscription-repositories
    baseUri: ''
    description: github-repos-api — Repositories business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-subscription
      path: /repos/{owner}/{repo}/subscription
      operations:
      - name: getrepositorysubscription
        method: GET
        description: GitHub Get Repository Subscription
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
      - name: setrepositorysubscription
        method: PUT
        description: GitHub Set Repository Subscription
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
      - name: deleterepositorysubscription
        method: DELETE
        description: GitHub Delete Repository Subscription
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-subscription-repositories-rest
    port: 8080
    description: REST adapter for github-repos-api — Repositories. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/subscription
      name: repos-owner-repo-subscription
      description: REST surface for repos-owner-repo-subscription.
      operations:
      - method: GET
        name: getrepositorysubscription
        description: GitHub Get Repository Subscription
        call: repo-subscription-repositories.getrepositorysubscription
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: setrepositorysubscription
        description: GitHub Set Repository Subscription
        call: repo-subscription-repositories.setrepositorysubscription
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deleterepositorysubscription
        description: GitHub Delete Repository Subscription
        call: repo-subscription-repositories.deleterepositorysubscription
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-subscription-repositories-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repos-api — Repositories. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: github-get-repository-subscription
      description: GitHub Get Repository Subscription
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: repo-subscription-repositories.getrepositorysubscription
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.
    - name: github-set-repository-subscription
      description: GitHub Set Repository Subscription
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: repo-subscription-repositories.setrepositorysubscription
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: github-delete-repository-subscription
      description: GitHub Delete Repository Subscription
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: repo-subscription-repositories.deleterepositorysubscription
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

naftiko: 1.0.0-alpha2
info:
  label: GitHub Org API — Name
  description: 'GitHub Org API — Name. 1 operations. Lead operation: GitHub Get Team by Name. Self-contained Naftiko capability
    covering one Github business surface.'
  tags:
  - Github
  - Name
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: organizations-name
    baseUri: ''
    description: GitHub Org API — Name business capability. Self-contained, no shared references.
    resources:
    - name: orgs-org-teams-team_slug
      path: /orgs/{org}/teams/{team_slug}
      operations:
      - name: getteambyname
        method: GET
        description: GitHub Get Team by Name
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: organizations-name-rest
    port: 8080
    description: REST adapter for GitHub Org API — Name. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/orgs/{org}/teams/{team-slug}
      name: orgs-org-teams-team-slug
      description: REST surface for orgs-org-teams-team_slug.
      operations:
      - method: GET
        name: getteambyname
        description: GitHub Get Team by Name
        call: organizations-name.getteambyname
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: organizations-name-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitHub Org API — Name. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: github-get-team-name
      description: GitHub Get Team by Name
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: organizations-name.getteambyname
      outputParameters:
      - type: object
        mapping: $.

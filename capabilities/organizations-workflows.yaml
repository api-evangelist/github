naftiko: 1.0.0-alpha2
info:
  label: GitHub Org API — Workflows
  description: 'GitHub Org API — Workflows. 2 operations. Lead operation: GitHub Get Default Workflow Permissions for an Organization.
    Self-contained Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Workflows
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: organizations-workflows
    baseUri: ''
    description: GitHub Org API — Workflows business capability. Self-contained, no shared references.
    resources:
    - name: orgs-org-actions-permissions-workflow
      path: /orgs/{org}/actions/permissions/workflow
      operations:
      - name: getdefaultworkflowpermissionsforanorganization
        method: GET
        description: GitHub Get Default Workflow Permissions for an Organization
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: setdefaultworkflowpermissionsforanorganization
        method: PUT
        description: GitHub Set Default Workflow Permissions for an Organization
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: organizations-workflows-rest
    port: 8080
    description: REST adapter for GitHub Org API — Workflows. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/orgs/{org}/actions/permissions/workflow
      name: orgs-org-actions-permissions-workflow
      description: REST surface for orgs-org-actions-permissions-workflow.
      operations:
      - method: GET
        name: getdefaultworkflowpermissionsforanorganization
        description: GitHub Get Default Workflow Permissions for an Organization
        call: organizations-workflows.getdefaultworkflowpermissionsforanorganization
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: setdefaultworkflowpermissionsforanorganization
        description: GitHub Set Default Workflow Permissions for an Organization
        call: organizations-workflows.setdefaultworkflowpermissionsforanorganization
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: organizations-workflows-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitHub Org API — Workflows. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: github-get-default-workflow-permissions
      description: GitHub Get Default Workflow Permissions for an Organization
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: organizations-workflows.getdefaultworkflowpermissionsforanorganization
      outputParameters:
      - type: object
        mapping: $.
    - name: github-set-default-workflow-permissions
      description: GitHub Set Default Workflow Permissions for an Organization
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: organizations-workflows.setdefaultworkflowpermissionsforanorganization
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.

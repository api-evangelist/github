naftiko: 1.0.0-alpha2
info:
  label: GitHub Repos API — Day
  description: 'GitHub Repos API — Day. 1 operations. Lead operation: Get The Hourly Commit Count For Each Day. Self-contained
    Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Day
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: temp-day
    baseUri: ''
    description: GitHub Repos API — Day business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-stats-punch_card
      path: /repos/{owner}/{repo}/stats/punch_card
      operations:
      - name: getthehourlycommitcountforeachday
        method: GET
        description: Get The Hourly Commit Count For Each Day
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: temp-day-rest
    port: 8080
    description: REST adapter for GitHub Repos API — Day. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/stats/punch-card
      name: repos-owner-repo-stats-punch-card
      description: REST surface for repos-owner-repo-stats-punch_card.
      operations:
      - method: GET
        name: getthehourlycommitcountforeachday
        description: Get The Hourly Commit Count For Each Day
        call: temp-day.getthehourlycommitcountforeachday
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: temp-day-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitHub Repos API — Day. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: get-hourly-commit-count-each
      description: Get The Hourly Commit Count For Each Day
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: temp-day.getthehourlycommitcountforeachday
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

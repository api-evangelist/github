naftiko: 1.0.0-alpha2
info:
  label: GitHub User API — Follow
  description: 'GitHub User API — Follow. 1 operations. Lead operation: GitHub Follow User. Self-contained Naftiko capability
    covering one Github business surface.'
  tags:
  - Github
  - Follow
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: users-follow
    baseUri: ''
    description: GitHub User API — Follow business capability. Self-contained, no shared references.
    resources:
    - name: user-following-username
      path: /user/following/{username}
      operations:
      - name: followuser
        method: PUT
        description: GitHub Follow User
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: users-follow-rest
    port: 8080
    description: REST adapter for GitHub User API — Follow. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/user/following/{username}
      name: user-following-username
      description: REST surface for user-following-username.
      operations:
      - method: PUT
        name: followuser
        description: GitHub Follow User
        call: users-follow.followuser
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: users-follow-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitHub User API — Follow. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: github-follow-user
      description: GitHub Follow User
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: users-follow.followuser
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

naftiko: 1.0.0-alpha2
info:
  label: github-repos-api — Name
  description: 'github-repos-api — Name. 1 operations. Lead operation: GitHub Get Release by Tag Name. Self-contained Naftiko
    capability covering one Github business surface.'
  tags:
  - Github
  - Name
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-tags-name
    baseUri: ''
    description: github-repos-api — Name business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-releases-tags-tag
      path: /repos/{owner}/{repo}/releases/tags/{tag}
      operations:
      - name: getreleasebytagname
        method: GET
        description: GitHub Get Release by Tag Name
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: tag
          in: path
          type: string
          description: tag parameter
          required: true
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-tags-name-rest
    port: 8080
    description: REST adapter for github-repos-api — Name. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/releases/tags/{tag}
      name: repos-owner-repo-releases-tags-tag
      description: REST surface for repos-owner-repo-releases-tags-tag.
      operations:
      - method: GET
        name: getreleasebytagname
        description: GitHub Get Release by Tag Name
        call: repo-tags-name.getreleasebytagname
        with:
          tag: rest.tag
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-tags-name-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repos-api — Name. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: github-get-release-tag-name
      description: GitHub Get Release by Tag Name
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: repo-tags-name.getreleasebytagname
      with:
        tag: tools.tag
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

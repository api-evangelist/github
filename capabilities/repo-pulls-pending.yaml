naftiko: 1.0.0-alpha2
info:
  label: github-repos-api — Pending
  description: 'github-repos-api — Pending. 1 operations. Lead operation: GitHub Delete Pending Review for Pull Request. Self-contained
    Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Pending
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-pulls-pending
    baseUri: ''
    description: github-repos-api — Pending business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-pulls-pull_number-reviews-review_id
      path: /repos/{owner}/{repo}/pulls/{pull_number}/reviews/{review_id}
      operations:
      - name: deletependingreviewforpullrequest
        method: DELETE
        description: GitHub Delete Pending Review for Pull Request
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-pulls-pending-rest
    port: 8080
    description: REST adapter for github-repos-api — Pending. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/pulls/{pull-number}/reviews/{review-id}
      name: repos-owner-repo-pulls-pull-number-reviews-review-id
      description: REST surface for repos-owner-repo-pulls-pull_number-reviews-review_id.
      operations:
      - method: DELETE
        name: deletependingreviewforpullrequest
        description: GitHub Delete Pending Review for Pull Request
        call: repo-pulls-pending.deletependingreviewforpullrequest
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-pulls-pending-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repos-api — Pending. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: github-delete-pending-review-pull
      description: GitHub Delete Pending Review for Pull Request
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: repo-pulls-pending.deletependingreviewforpullrequest
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

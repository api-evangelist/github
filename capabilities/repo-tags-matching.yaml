naftiko: 1.0.0-alpha2
info:
  label: github-repos-api — Matching
  description: 'github-repos-api — Matching. 1 operations. Lead operation: GitHub List Matching References. Self-contained
    Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Matching
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-tags-matching
    baseUri: ''
    description: github-repos-api — Matching business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-git-matching-refs-ref
      path: /repos/{owner}/{repo}/git/matching-refs/{ref}
      operations:
      - name: listmatchingreferences
        method: GET
        description: GitHub List Matching References
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-tags-matching-rest
    port: 8080
    description: REST adapter for github-repos-api — Matching. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/git/matching-refs/{ref}
      name: repos-owner-repo-git-matching-refs-ref
      description: REST surface for repos-owner-repo-git-matching-refs-ref.
      operations:
      - method: GET
        name: listmatchingreferences
        description: GitHub List Matching References
        call: repo-tags-matching.listmatchingreferences
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-tags-matching-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repos-api — Matching. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: github-list-matching-references
      description: GitHub List Matching References
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: repo-tags-matching.listmatchingreferences
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

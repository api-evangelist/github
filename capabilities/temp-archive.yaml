naftiko: 1.0.0-alpha2
info:
  label: GitHub Repos API — Archive
  description: 'GitHub Repos API — Archive. 1 operations. Lead operation: Download Repository Archive (tar). Self-contained
    Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Archive
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: temp-archive
    baseUri: ''
    description: GitHub Repos API — Archive business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-tarball-ref
      path: /repos/{owner}/{repo}/tarball/{ref}
      operations:
      - name: downloadrepositoryarchivetar
        method: GET
        description: Download Repository Archive (tar)
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: ref
          in: path
          type: string
          required: true
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: temp-archive-rest
    port: 8080
    description: REST adapter for GitHub Repos API — Archive. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/tarball/{ref}
      name: repos-owner-repo-tarball-ref
      description: REST surface for repos-owner-repo-tarball-ref.
      operations:
      - method: GET
        name: downloadrepositoryarchivetar
        description: Download Repository Archive (tar)
        call: temp-archive.downloadrepositoryarchivetar
        with:
          ref: rest.ref
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: temp-archive-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitHub Repos API — Archive. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: download-repository-archive-tar
      description: Download Repository Archive (tar)
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: temp-archive.downloadrepositoryarchivetar
      with:
        ref: tools.ref
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

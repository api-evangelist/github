naftiko: 1.0.0-alpha2
info:
  label: github-repos-api — Create
  description: 'github-repos-api — Create. 1 operations. Lead operation: GitHub Create an Autolink Reference for Repository.
    Self-contained Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Create
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-autolinks-create
    baseUri: ''
    description: github-repos-api — Create business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-autolinks
      path: /repos/{owner}/{repo}/autolinks
      operations:
      - name: createanautolinkreferenceforrepository
        method: POST
        description: GitHub Create an Autolink Reference for Repository
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-autolinks-create-rest
    port: 8080
    description: REST adapter for github-repos-api — Create. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/autolinks
      name: repos-owner-repo-autolinks
      description: REST surface for repos-owner-repo-autolinks.
      operations:
      - method: POST
        name: createanautolinkreferenceforrepository
        description: GitHub Create an Autolink Reference for Repository
        call: repo-autolinks-create.createanautolinkreferenceforrepository
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-autolinks-create-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repos-api — Create. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: github-create-autolink-reference-repository
      description: GitHub Create an Autolink Reference for Repository
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: repo-autolinks-create.createanautolinkreferenceforrepository
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.

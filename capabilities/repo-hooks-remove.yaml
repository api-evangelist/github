naftiko: 1.0.0-alpha2
info:
  label: github-repo-hooks-api — Remove
  description: 'github-repo-hooks-api — Remove. 1 operations. Lead operation: GitHub Remove Pre-receive Hook Enforcement for
    Repository. Self-contained Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Remove
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-hooks-remove
    baseUri: ''
    description: github-repo-hooks-api — Remove business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-pre-receive-hooks-pre_receive_hook_id
      path: /repos/{owner}/{repo}/pre-receive-hooks/{pre_receive_hook_id}
      operations:
      - name: removeprereceivehookenforcementforrepository
        method: DELETE
        description: GitHub Remove Pre-receive Hook Enforcement for Repository
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-hooks-remove-rest
    port: 8080
    description: REST adapter for github-repo-hooks-api — Remove. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/pre-receive-hooks/{pre-receive-hook-id}
      name: repos-owner-repo-pre-receive-hooks-pre-receive-hook-id
      description: REST surface for repos-owner-repo-pre-receive-hooks-pre_receive_hook_id.
      operations:
      - method: DELETE
        name: removeprereceivehookenforcementforrepository
        description: GitHub Remove Pre-receive Hook Enforcement for Repository
        call: repo-hooks-remove.removeprereceivehookenforcementforrepository
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-hooks-remove-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repo-hooks-api — Remove. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: github-remove-pre-receive-hook
      description: GitHub Remove Pre-receive Hook Enforcement for Repository
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: repo-hooks-remove.removeprereceivehookenforcementforrepository
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

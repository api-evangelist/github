naftiko: 1.0.0-alpha2
info:
  label: github-repo-hooks-api — Re-Deliver
  description: 'github-repo-hooks-api — Re-Deliver. 1 operations. Lead operation: GitHub Redeliver Delivery for Repository
    Webhook. Self-contained Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Re-Deliver
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-hooks-re-deliver
    baseUri: ''
    description: github-repo-hooks-api — Re-Deliver business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-hooks-hook_id-deliveries-delivery_id-attempts
      path: /repos/{owner}/{repo}/hooks/{hook_id}/deliveries/{delivery_id}/attempts
      operations:
      - name: redeliverdeliveryforrepositorywebhook
        method: POST
        description: GitHub Redeliver Delivery for Repository Webhook
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-hooks-re-deliver-rest
    port: 8080
    description: REST adapter for github-repo-hooks-api — Re-Deliver. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/hooks/{hook-id}/deliveries/{delivery-id}/attempts
      name: repos-owner-repo-hooks-hook-id-deliveries-delivery-id-attempts
      description: REST surface for repos-owner-repo-hooks-hook_id-deliveries-delivery_id-attempts.
      operations:
      - method: POST
        name: redeliverdeliveryforrepositorywebhook
        description: GitHub Redeliver Delivery for Repository Webhook
        call: repo-hooks-re-deliver.redeliverdeliveryforrepositorywebhook
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-hooks-re-deliver-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repo-hooks-api — Re-Deliver. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: github-redeliver-delivery-repository-webhook
      description: GitHub Redeliver Delivery for Repository Webhook
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: repo-hooks-re-deliver.redeliverdeliveryforrepositorywebhook
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

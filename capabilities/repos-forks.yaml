naftiko: 1.0.0-alpha2
info:
  label: github-repos-api — Forks
  description: 'github-repos-api — Forks. 2 operations. Lead operation: GitHub List Forks. Self-contained Naftiko capability
    covering one Github business surface.'
  tags:
  - Github
  - Forks
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repos-forks
    baseUri: http://localhost:8080/rest/github-repos-api/1.1.4
    description: github-repos-api — Forks business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-forks
      path: /repos/{owner}/{repo}/forks
      operations:
      - name: listforks
        method: GET
        description: GitHub List Forks
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: sort
          in: query
          type: string
          description: The sort order. `stargazers` will sort by star count.
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
      - name: createfork
        method: POST
        description: GitHub Create Fork
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repos-forks-rest
    port: 8080
    description: REST adapter for github-repos-api — Forks. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/forks
      name: repos-owner-repo-forks
      description: REST surface for repos-owner-repo-forks.
      operations:
      - method: GET
        name: listforks
        description: GitHub List Forks
        call: repos-forks.listforks
        with:
          sort: rest.sort
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createfork
        description: GitHub Create Fork
        call: repos-forks.createfork
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repos-forks-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repos-api — Forks. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: github-list-forks
      description: GitHub List Forks
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: repos-forks.listforks
      with:
        sort: tools.sort
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.
    - name: github-create-fork
      description: GitHub Create Fork
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: repos-forks.createfork
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.

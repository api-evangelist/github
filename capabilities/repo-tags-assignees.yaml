naftiko: 1.0.0-alpha2
info:
  label: github-repos-api — Assignees
  description: 'github-repos-api — Assignees. 1 operations. Lead operation: GitHub List Assignees. Self-contained Naftiko
    capability covering one Github business surface.'
  tags:
  - Github
  - Assignees
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-tags-assignees
    baseUri: ''
    description: github-repos-api — Assignees business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-assignees
      path: /repos/{owner}/{repo}/assignees
      operations:
      - name: listassignees
        method: GET
        description: GitHub List Assignees
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-tags-assignees-rest
    port: 8080
    description: REST adapter for github-repos-api — Assignees. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/assignees
      name: repos-owner-repo-assignees
      description: REST surface for repos-owner-repo-assignees.
      operations:
      - method: GET
        name: listassignees
        description: GitHub List Assignees
        call: repo-tags-assignees.listassignees
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-tags-assignees-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repos-api — Assignees. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: github-list-assignees
      description: GitHub List Assignees
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: repo-tags-assignees.listassignees
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

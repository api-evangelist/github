naftiko: 1.0.0-alpha2
info:
  label: GitHub Repos API — Openid Connect
  description: 'GitHub Repos API — Openid Connect. 2 operations. Lead operation: Get The Customization Template For An Oidc
    Subject Claim For Repository. Self-contained Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Openid Connect
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: temp-openid-connect
    baseUri: ''
    description: GitHub Repos API — Openid Connect business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-actions-oidc-customization-sub
      path: /repos/{owner}/{repo}/actions/oidc/customization/sub
      operations:
      - name: getthecustomizationtemplateforanoidcsubjectclaimforrepository
        method: GET
        description: Get The Customization Template For An Oidc Subject Claim For Repository
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
      - name: setthecustomizationtemplateforanoidcsubjectclaimforrepository
        method: PUT
        description: Set The Customization Template For An Oidc Subject Claim For Repository
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: temp-openid-connect-rest
    port: 8080
    description: REST adapter for GitHub Repos API — Openid Connect. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/actions/oidc/customization/sub
      name: repos-owner-repo-actions-oidc-customization-sub
      description: REST surface for repos-owner-repo-actions-oidc-customization-sub.
      operations:
      - method: GET
        name: getthecustomizationtemplateforanoidcsubjectclaimforrepository
        description: Get The Customization Template For An Oidc Subject Claim For Repository
        call: temp-openid-connect.getthecustomizationtemplateforanoidcsubjectclaimforrepository
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: setthecustomizationtemplateforanoidcsubjectclaimforrepository
        description: Set The Customization Template For An Oidc Subject Claim For Repository
        call: temp-openid-connect.setthecustomizationtemplateforanoidcsubjectclaimforrepository
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: temp-openid-connect-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitHub Repos API — Openid Connect. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: get-customization-template-oidc-subject
      description: Get The Customization Template For An Oidc Subject Claim For Repository
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: temp-openid-connect.getthecustomizationtemplateforanoidcsubjectclaimforrepository
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.
    - name: set-customization-template-oidc-subject
      description: Set The Customization Template For An Oidc Subject Claim For Repository
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: temp-openid-connect.setthecustomizationtemplateforanoidcsubjectclaimforrepository
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.

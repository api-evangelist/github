naftiko: 1.0.0-alpha2
info:
  label: github-repos-api — Stargazers
  description: 'github-repos-api — Stargazers. 1 operations. Lead operation: GitHub List Stargazers. Self-contained Naftiko
    capability covering one Github business surface.'
  tags:
  - Github
  - Stargazers
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-tags-stargazers
    baseUri: ''
    description: github-repos-api — Stargazers business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-stargazers
      path: /repos/{owner}/{repo}/stargazers
      operations:
      - name: liststargazers
        method: GET
        description: GitHub List Stargazers
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-tags-stargazers-rest
    port: 8080
    description: REST adapter for github-repos-api — Stargazers. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/stargazers
      name: repos-owner-repo-stargazers
      description: REST surface for repos-owner-repo-stargazers.
      operations:
      - method: GET
        name: liststargazers
        description: GitHub List Stargazers
        call: repo-tags-stargazers.liststargazers
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-tags-stargazers-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repos-api — Stargazers. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: github-list-stargazers
      description: GitHub List Stargazers
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: repo-tags-stargazers.liststargazers
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

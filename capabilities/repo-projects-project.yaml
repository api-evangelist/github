naftiko: 1.0.0-alpha2
info:
  label: github-repos-api — Project
  description: 'github-repos-api — Project. 1 operations. Lead operation: GitHub Create Repository Project. Self-contained
    Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Project
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-projects-project
    baseUri: ''
    description: github-repos-api — Project business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-projects
      path: /repos/{owner}/{repo}/projects
      operations:
      - name: createrepositoryproject
        method: POST
        description: GitHub Create Repository Project
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-projects-project-rest
    port: 8080
    description: REST adapter for github-repos-api — Project. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/projects
      name: repos-owner-repo-projects
      description: REST surface for repos-owner-repo-projects.
      operations:
      - method: POST
        name: createrepositoryproject
        description: GitHub Create Repository Project
        call: repo-projects-project.createrepositoryproject
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-projects-project-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repos-api — Project. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: github-create-repository-project
      description: GitHub Create Repository Project
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: repo-projects-project.createrepositoryproject
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.

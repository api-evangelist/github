naftiko: 1.0.0-alpha2
info:
  label: GitHub Repos API — Count
  description: 'GitHub Repos API — Count. 2 operations. Lead operation: Get The Weekly Commit Count. Self-contained Naftiko
    capability covering one Github business surface.'
  tags:
  - Github
  - Count
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: temp-count
    baseUri: ''
    description: GitHub Repos API — Count business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-stats-participation
      path: /repos/{owner}/{repo}/stats/participation
      operations:
      - name: gettheweeklycommitcount
        method: GET
        description: Get The Weekly Commit Count
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    - name: repos-owner-repo-stats-punch_card
      path: /repos/{owner}/{repo}/stats/punch_card
      operations:
      - name: getthehourlycommitcountforeachday
        method: GET
        description: Get The Hourly Commit Count For Each Day
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: temp-count-rest
    port: 8080
    description: REST adapter for GitHub Repos API — Count. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/stats/participation
      name: repos-owner-repo-stats-participation
      description: REST surface for repos-owner-repo-stats-participation.
      operations:
      - method: GET
        name: gettheweeklycommitcount
        description: Get The Weekly Commit Count
        call: temp-count.gettheweeklycommitcount
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/repos/{owner}/{repo}/stats/punch-card
      name: repos-owner-repo-stats-punch-card
      description: REST surface for repos-owner-repo-stats-punch_card.
      operations:
      - method: GET
        name: getthehourlycommitcountforeachday
        description: Get The Hourly Commit Count For Each Day
        call: temp-count.getthehourlycommitcountforeachday
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: temp-count-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitHub Repos API — Count. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: get-weekly-commit-count
      description: Get The Weekly Commit Count
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: temp-count.gettheweeklycommitcount
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.
    - name: get-hourly-commit-count-each
      description: Get The Hourly Commit Count For Each Day
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: temp-count.getthehourlycommitcountforeachday
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

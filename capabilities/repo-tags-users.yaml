naftiko: 1.0.0-alpha2
info:
  label: github-repos-api — Users
  description: 'github-repos-api — Users. 2 operations. Lead operation: GitHub Check if User Can Be Assigned. Self-contained
    Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Users
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-tags-users
    baseUri: ''
    description: github-repos-api — Users business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-assignees-assignee
      path: /repos/{owner}/{repo}/assignees/{assignee}
      operations:
      - name: checkifusercanbeassigned
        method: GET
        description: GitHub Check if User Can Be Assigned
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: assignee
          in: path
          type: string
          required: true
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    - name: repos-owner-repo-notifications
      path: /repos/{owner}/{repo}/notifications
      operations:
      - name: listrepositorynotificationsfortheauthenticateduser
        method: GET
        description: GitHub List Repository Notifications for the Authenticated User
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-tags-users-rest
    port: 8080
    description: REST adapter for github-repos-api — Users. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/assignees/{assignee}
      name: repos-owner-repo-assignees-assignee
      description: REST surface for repos-owner-repo-assignees-assignee.
      operations:
      - method: GET
        name: checkifusercanbeassigned
        description: GitHub Check if User Can Be Assigned
        call: repo-tags-users.checkifusercanbeassigned
        with:
          assignee: rest.assignee
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/repos/{owner}/{repo}/notifications
      name: repos-owner-repo-notifications
      description: REST surface for repos-owner-repo-notifications.
      operations:
      - method: GET
        name: listrepositorynotificationsfortheauthenticateduser
        description: GitHub List Repository Notifications for the Authenticated User
        call: repo-tags-users.listrepositorynotificationsfortheauthenticateduser
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-tags-users-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repos-api — Users. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: github-check-if-user-can
      description: GitHub Check if User Can Be Assigned
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: repo-tags-users.checkifusercanbeassigned
      with:
        assignee: tools.assignee
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.
    - name: github-list-repository-notifications-authenticated
      description: GitHub List Repository Notifications for the Authenticated User
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: repo-tags-users.listrepositorynotificationsfortheauthenticateduser
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

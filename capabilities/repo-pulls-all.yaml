naftiko: 1.0.0-alpha2
info:
  label: github-repos-api — All
  description: 'github-repos-api — All. 1 operations. Lead operation: GitHub Get All Requested Reviewers for Pull Request.
    Self-contained Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - All
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-pulls-all
    baseUri: ''
    description: github-repos-api — All business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-pulls-pull_number-requested_reviewers
      path: /repos/{owner}/{repo}/pulls/{pull_number}/requested_reviewers
      operations:
      - name: getallrequestedreviewersforpullrequest
        method: GET
        description: GitHub Get All Requested Reviewers for Pull Request
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-pulls-all-rest
    port: 8080
    description: REST adapter for github-repos-api — All. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/pulls/{pull-number}/requested-reviewers
      name: repos-owner-repo-pulls-pull-number-requested-reviewers
      description: REST surface for repos-owner-repo-pulls-pull_number-requested_reviewers.
      operations:
      - method: GET
        name: getallrequestedreviewersforpullrequest
        description: GitHub Get All Requested Reviewers for Pull Request
        call: repo-pulls-all.getallrequestedreviewersforpullrequest
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-pulls-all-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repos-api — All. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: github-get-all-requested-reviewers
      description: GitHub Get All Requested Reviewers for Pull Request
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: repo-pulls-all.getallrequestedreviewersforpullrequest
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

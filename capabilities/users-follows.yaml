naftiko: 1.0.0-alpha2
info:
  label: GitHub User API — Follows
  description: 'GitHub User API — Follows. 3 operations. Lead operation: GitHub List the People the Authenticated User Follows.
    Self-contained Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Follows
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: users-follows
    baseUri: ''
    description: GitHub User API — Follows business capability. Self-contained, no shared references.
    resources:
    - name: user-following
      path: /user/following
      operations:
      - name: listthepeopletheauthenticateduserfollows
        method: GET
        description: GitHub List the People the Authenticated User Follows
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    - name: users-username-following
      path: /users/{username}/following
      operations:
      - name: listthepeopleuserfollows
        method: GET
        description: GitHub List the People User Follows
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    - name: users-username-following-target_user
      path: /users/{username}/following/{target_user}
      operations:
      - name: checkifuserfollowsanotheruser
        method: GET
        description: GitHub Check if User Follows Another User
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: target_user
          in: path
          type: string
          required: true
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: users-follows-rest
    port: 8080
    description: REST adapter for GitHub User API — Follows. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/user/following
      name: user-following
      description: REST surface for user-following.
      operations:
      - method: GET
        name: listthepeopletheauthenticateduserfollows
        description: GitHub List the People the Authenticated User Follows
        call: users-follows.listthepeopletheauthenticateduserfollows
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/users/{username}/following
      name: users-username-following
      description: REST surface for users-username-following.
      operations:
      - method: GET
        name: listthepeopleuserfollows
        description: GitHub List the People User Follows
        call: users-follows.listthepeopleuserfollows
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/users/{username}/following/{target-user}
      name: users-username-following-target-user
      description: REST surface for users-username-following-target_user.
      operations:
      - method: GET
        name: checkifuserfollowsanotheruser
        description: GitHub Check if User Follows Another User
        call: users-follows.checkifuserfollowsanotheruser
        with:
          target_user: rest.target_user
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: users-follows-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitHub User API — Follows. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: github-list-people-authenticated-user
      description: GitHub List the People the Authenticated User Follows
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: users-follows.listthepeopletheauthenticateduserfollows
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.
    - name: github-list-people-user-follows
      description: GitHub List the People User Follows
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: users-follows.listthepeopleuserfollows
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.
    - name: github-check-if-user-follows
      description: GitHub Check if User Follows Another User
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: users-follows.checkifuserfollowsanotheruser
      with:
        target_user: tools.target_user
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

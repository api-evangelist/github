naftiko: 1.0.0-alpha2
info:
  label: GitHub Repos API — Associated
  description: 'GitHub Repos API — Associated. 1 operations. Lead operation: List Pull Requests Associated With Commit. Self-contained
    Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Associated
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: temp-associated
    baseUri: ''
    description: GitHub Repos API — Associated business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-commits-commit_sha-pulls
      path: /repos/{owner}/{repo}/commits/{commit_sha}/pulls
      operations:
      - name: listpullrequestsassociatedwithcommit
        method: GET
        description: List Pull Requests Associated With Commit
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: temp-associated-rest
    port: 8080
    description: REST adapter for GitHub Repos API — Associated. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/commits/{commit-sha}/pulls
      name: repos-owner-repo-commits-commit-sha-pulls
      description: REST surface for repos-owner-repo-commits-commit_sha-pulls.
      operations:
      - method: GET
        name: listpullrequestsassociatedwithcommit
        description: List Pull Requests Associated With Commit
        call: temp-associated.listpullrequestsassociatedwithcommit
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: temp-associated-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitHub Repos API — Associated. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: list-pull-requests-associated-commit
      description: List Pull Requests Associated With Commit
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: temp-associated.listpullrequestsassociatedwithcommit
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

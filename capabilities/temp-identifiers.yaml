naftiko: 1.0.0-alpha2
info:
  label: GitHub Repos API — Identifiers
  description: 'GitHub Repos API — Identifiers. 1 operations. Lead operation: Delete Github Actions Cache For Repository (using
    Cache Id). Self-contained Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Identifiers
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: temp-identifiers
    baseUri: ''
    description: GitHub Repos API — Identifiers business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-actions-caches-cache_id
      path: /repos/{owner}/{repo}/actions/caches/{cache_id}
      operations:
      - name: deletegithubactionscacheforrepositoryusingcacheid
        method: DELETE
        description: Delete Github Actions Cache For Repository (using Cache Id)
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: temp-identifiers-rest
    port: 8080
    description: REST adapter for GitHub Repos API — Identifiers. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/actions/caches/{cache-id}
      name: repos-owner-repo-actions-caches-cache-id
      description: REST surface for repos-owner-repo-actions-caches-cache_id.
      operations:
      - method: DELETE
        name: deletegithubactionscacheforrepositoryusingcacheid
        description: Delete Github Actions Cache For Repository (using Cache Id)
        call: temp-identifiers.deletegithubactionscacheforrepositoryusingcacheid
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: temp-identifiers-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitHub Repos API — Identifiers. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: delete-github-actions-cache-repository
      description: Delete Github Actions Cache For Repository (using Cache Id)
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: temp-identifiers.deletegithubactionscacheforrepositoryusingcacheid
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

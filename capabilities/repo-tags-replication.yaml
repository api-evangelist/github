naftiko: 1.0.0-alpha2
info:
  label: github-repos-api — Replication
  description: 'github-repos-api — Replication. 1 operations. Lead operation: GitHub List Repository Cache Replication Status.
    Self-contained Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Replication
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-tags-replication
    baseUri: ''
    description: github-repos-api — Replication business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-replicas-caches
      path: /repos/{owner}/{repo}/replicas/caches
      operations:
      - name: listrepositorycachereplicationstatus
        method: GET
        description: GitHub List Repository Cache Replication Status
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-tags-replication-rest
    port: 8080
    description: REST adapter for github-repos-api — Replication. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/replicas/caches
      name: repos-owner-repo-replicas-caches
      description: REST surface for repos-owner-repo-replicas-caches.
      operations:
      - method: GET
        name: listrepositorycachereplicationstatus
        description: GitHub List Repository Cache Replication Status
        call: repo-tags-replication.listrepositorycachereplicationstatus
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-tags-replication-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repos-api — Replication. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: github-list-repository-cache-replication
      description: GitHub List Repository Cache Replication Status
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: repo-tags-replication.listrepositorycachereplicationstatus
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

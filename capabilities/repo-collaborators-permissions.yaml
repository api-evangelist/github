naftiko: 1.0.0-alpha2
info:
  label: github-repos-api — Permissions
  description: 'github-repos-api — Permissions. 1 operations. Lead operation: GitHub Get Repository Permissions for User.
    Self-contained Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Permissions
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-collaborators-permissions
    baseUri: ''
    description: github-repos-api — Permissions business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-collaborators-username-permission
      path: /repos/{owner}/{repo}/collaborators/{username}/permission
      operations:
      - name: getrepositorypermissionsforuser
        method: GET
        description: GitHub Get Repository Permissions for User
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-collaborators-permissions-rest
    port: 8080
    description: REST adapter for github-repos-api — Permissions. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/collaborators/{username}/permission
      name: repos-owner-repo-collaborators-username-permission
      description: REST surface for repos-owner-repo-collaborators-username-permission.
      operations:
      - method: GET
        name: getrepositorypermissionsforuser
        description: GitHub Get Repository Permissions for User
        call: repo-collaborators-permissions.getrepositorypermissionsforuser
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-collaborators-permissions-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repos-api — Permissions. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: github-get-repository-permissions-user
      description: GitHub Get Repository Permissions for User
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: repo-collaborators-permissions.getrepositorypermissionsforuser
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

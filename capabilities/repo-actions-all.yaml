naftiko: 1.0.0-alpha2
info:
  label: github-repo-actions-api — All
  description: 'github-repo-actions-api — All. 1 operations. Lead operation: GitHub Remove All Custom Labels from Self-hosted
    Runner for Repository. Self-contained Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - All
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-actions-all
    baseUri: ''
    description: github-repo-actions-api — All business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-actions-runners-runner_id-labels
      path: /repos/{owner}/{repo}/actions/runners/{runner_id}/labels
      operations:
      - name: removeallcustomlabelsfromselfhostedrunnerforrepository
        method: DELETE
        description: GitHub Remove All Custom Labels from Self-hosted Runner for Repository
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-actions-all-rest
    port: 8080
    description: REST adapter for github-repo-actions-api — All. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/actions/runners/{runner-id}/labels
      name: repos-owner-repo-actions-runners-runner-id-labels
      description: REST surface for repos-owner-repo-actions-runners-runner_id-labels.
      operations:
      - method: DELETE
        name: removeallcustomlabelsfromselfhostedrunnerforrepository
        description: GitHub Remove All Custom Labels from Self-hosted Runner for Repository
        call: repo-actions-all.removeallcustomlabelsfromselfhostedrunnerforrepository
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-actions-all-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repo-actions-api — All. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: github-remove-all-custom-labels
      description: GitHub Remove All Custom Labels from Self-hosted Runner for Repository
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: repo-actions-all.removeallcustomlabelsfromselfhostedrunnerforrepository
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

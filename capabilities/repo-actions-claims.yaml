naftiko: 1.0.0-alpha2
info:
  label: github-repo-actions-api — Claims
  description: 'github-repo-actions-api — Claims. 2 operations. Lead operation: GitHub Get the Customization Template for
    an Oidc Subject Claim for Repository. Self-contained Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Claims
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-actions-claims
    baseUri: ''
    description: github-repo-actions-api — Claims business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-actions-oidc-customization-sub
      path: /repos/{owner}/{repo}/actions/oidc/customization/sub
      operations:
      - name: getthecustomizationtemplateforanoidcsubjectclaimforrepository
        method: GET
        description: GitHub Get the Customization Template for an Oidc Subject Claim for Repository
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
      - name: setthecustomizationtemplateforanoidcsubjectclaimforrepository
        method: PUT
        description: GitHub Set the Customization Template for an Oidc Subject Claim for Repository
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-actions-claims-rest
    port: 8080
    description: REST adapter for github-repo-actions-api — Claims. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/actions/oidc/customization/sub
      name: repos-owner-repo-actions-oidc-customization-sub
      description: REST surface for repos-owner-repo-actions-oidc-customization-sub.
      operations:
      - method: GET
        name: getthecustomizationtemplateforanoidcsubjectclaimforrepository
        description: GitHub Get the Customization Template for an Oidc Subject Claim for Repository
        call: repo-actions-claims.getthecustomizationtemplateforanoidcsubjectclaimforrepository
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: setthecustomizationtemplateforanoidcsubjectclaimforrepository
        description: GitHub Set the Customization Template for an Oidc Subject Claim for Repository
        call: repo-actions-claims.setthecustomizationtemplateforanoidcsubjectclaimforrepository
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-actions-claims-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repo-actions-api — Claims. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: github-get-customization-template-oidc
      description: GitHub Get the Customization Template for an Oidc Subject Claim for Repository
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: repo-actions-claims.getthecustomizationtemplateforanoidcsubjectclaimforrepository
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.
    - name: github-set-customization-template-oidc
      description: GitHub Set the Customization Template for an Oidc Subject Claim for Repository
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: repo-actions-claims.setthecustomizationtemplateforanoidcsubjectclaimforrepository
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.

naftiko: 1.0.0-alpha2
info:
  label: github-repos-api — About
  description: 'github-repos-api — About. 1 operations. Lead operation: GitHub Get Information About Sarif Upload. Self-contained
    Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - About
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-code-scanning-about
    baseUri: ''
    description: github-repos-api — About business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-code-scanning-sarifs-sarif_id
      path: /repos/{owner}/{repo}/code-scanning/sarifs/{sarif_id}
      operations:
      - name: getinformationaboutsarifupload
        method: GET
        description: GitHub Get Information About Sarif Upload
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: sarif_id
          in: path
          type: string
          description: The SARIF ID obtained after uploading.
          required: true
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-code-scanning-about-rest
    port: 8080
    description: REST adapter for github-repos-api — About. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/code-scanning/sarifs/{sarif-id}
      name: repos-owner-repo-code-scanning-sarifs-sarif-id
      description: REST surface for repos-owner-repo-code-scanning-sarifs-sarif_id.
      operations:
      - method: GET
        name: getinformationaboutsarifupload
        description: GitHub Get Information About Sarif Upload
        call: repo-code-scanning-about.getinformationaboutsarifupload
        with:
          sarif_id: rest.sarif_id
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-code-scanning-about-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repos-api — About. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: github-get-information-about-sarif
      description: GitHub Get Information About Sarif Upload
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: repo-code-scanning-about.getinformationaboutsarifupload
      with:
        sarif_id: tools.sarif_id
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

naftiko: 1.0.0-alpha2
info:
  label: GitHub Projects API — Delete
  description: 'GitHub Projects API — Delete. 3 operations. Lead operation: GitHub Delete Project Card. Self-contained Naftiko
    capability covering one Github business surface.'
  tags:
  - Github
  - Delete
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: projects-delete
    baseUri: ''
    description: GitHub Projects API — Delete business capability. Self-contained, no shared references.
    resources:
    - name: projects-columns-cards-card_id
      path: /projects/columns/cards/{card_id}
      operations:
      - name: deleteprojectcard
        method: DELETE
        description: GitHub Delete Project Card
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: projects-columns-column_id
      path: /projects/columns/{column_id}
      operations:
      - name: deleteprojectcolumn
        method: DELETE
        description: GitHub Delete Project Column
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: projects-project_id
      path: /projects/{project_id}
      operations:
      - name: deleteproject
        method: DELETE
        description: GitHub Delete Project
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: projects-delete-rest
    port: 8080
    description: REST adapter for GitHub Projects API — Delete. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/projects/columns/cards/{card-id}
      name: projects-columns-cards-card-id
      description: REST surface for projects-columns-cards-card_id.
      operations:
      - method: DELETE
        name: deleteprojectcard
        description: GitHub Delete Project Card
        call: projects-delete.deleteprojectcard
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/projects/columns/{column-id}
      name: projects-columns-column-id
      description: REST surface for projects-columns-column_id.
      operations:
      - method: DELETE
        name: deleteprojectcolumn
        description: GitHub Delete Project Column
        call: projects-delete.deleteprojectcolumn
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/projects/{project-id}
      name: projects-project-id
      description: REST surface for projects-project_id.
      operations:
      - method: DELETE
        name: deleteproject
        description: GitHub Delete Project
        call: projects-delete.deleteproject
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: projects-delete-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitHub Projects API — Delete. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: github-delete-project-card
      description: GitHub Delete Project Card
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: projects-delete.deleteprojectcard
      outputParameters:
      - type: object
        mapping: $.
    - name: github-delete-project-column
      description: GitHub Delete Project Column
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: projects-delete.deleteprojectcolumn
      outputParameters:
      - type: object
        mapping: $.
    - name: github-delete-project
      description: GitHub Delete Project
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: projects-delete.deleteproject
      outputParameters:
      - type: object
        mapping: $.

naftiko: 1.0.0-alpha2
info:
  label: github-repo-actions-api — Identifiers
  description: 'github-repo-actions-api — Identifiers. 1 operations. Lead operation: GitHub Delete Github Actions Cache for
    Repository (using Cache Id). Self-contained Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Identifiers
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-actions-identifiers
    baseUri: ''
    description: github-repo-actions-api — Identifiers business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-actions-caches-cache_id
      path: /repos/{owner}/{repo}/actions/caches/{cache_id}
      operations:
      - name: deletegithubactionscacheforrepositoryusingcacheid
        method: DELETE
        description: GitHub Delete Github Actions Cache for Repository (using Cache Id)
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-actions-identifiers-rest
    port: 8080
    description: REST adapter for github-repo-actions-api — Identifiers. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/actions/caches/{cache-id}
      name: repos-owner-repo-actions-caches-cache-id
      description: REST surface for repos-owner-repo-actions-caches-cache_id.
      operations:
      - method: DELETE
        name: deletegithubactionscacheforrepositoryusingcacheid
        description: GitHub Delete Github Actions Cache for Repository (using Cache Id)
        call: repo-actions-identifiers.deletegithubactionscacheforrepositoryusingcacheid
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-actions-identifiers-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repo-actions-api — Identifiers. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: github-delete-github-actions-cache
      description: GitHub Delete Github Actions Cache for Repository (using Cache Id)
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: repo-actions-identifiers.deletegithubactionscacheforrepositoryusingcacheid
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

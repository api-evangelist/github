naftiko: 1.0.0-alpha2
info:
  label: github-repos-api — CODEOWNERS
  description: 'github-repos-api — CODEOWNERS. 1 operations. Lead operation: GitHub List Codeowners Errors. Self-contained
    Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - CODEOWNERS
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repos-codeowners
    baseUri: http://localhost:8080/rest/github-repos-api/1.1.4
    description: github-repos-api — CODEOWNERS business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-codeowners-errors
      path: /repos/{owner}/{repo}/codeowners/errors
      operations:
      - name: listcodeownerserrors
        method: GET
        description: GitHub List Codeowners Errors
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: ref
          in: query
          type: string
          description: 'branch, tag or commit name used to determine which version of the CODEOWNERS file to use. Default:
            the repository''s default branch (e.g. `main`)'
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repos-codeowners-rest
    port: 8080
    description: REST adapter for github-repos-api — CODEOWNERS. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/codeowners/errors
      name: repos-owner-repo-codeowners-errors
      description: REST surface for repos-owner-repo-codeowners-errors.
      operations:
      - method: GET
        name: listcodeownerserrors
        description: GitHub List Codeowners Errors
        call: repos-codeowners.listcodeownerserrors
        with:
          ref: rest.ref
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repos-codeowners-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repos-api — CODEOWNERS. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: github-list-codeowners-errors
      description: GitHub List Codeowners Errors
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: repos-codeowners.listcodeownerserrors
      with:
        ref: tools.ref
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

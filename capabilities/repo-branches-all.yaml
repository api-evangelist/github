naftiko: 1.0.0-alpha2
info:
  label: github-repos-api — All
  description: 'github-repos-api — All. 1 operations. Lead operation: GitHub Get All Status Check Contexts. Self-contained
    Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - All
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-branches-all
    baseUri: ''
    description: github-repos-api — All business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-branches-branch-protection-required_status_checks-contexts
      path: /repos/{owner}/{repo}/branches/{branch}/protection/required_status_checks/contexts
      operations:
      - name: getallstatuscheckcontexts
        method: GET
        description: GitHub Get All Status Check Contexts
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-branches-all-rest
    port: 8080
    description: REST adapter for github-repos-api — All. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/branches/{branch}/protection/required-status-checks/contexts
      name: repos-owner-repo-branches-branch-protection-required-status-checks-contexts
      description: REST surface for repos-owner-repo-branches-branch-protection-required_status_checks-contexts.
      operations:
      - method: GET
        name: getallstatuscheckcontexts
        description: GitHub Get All Status Check Contexts
        call: repo-branches-all.getallstatuscheckcontexts
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-branches-all-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repos-api — All. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: github-get-all-status-check
      description: GitHub Get All Status Check Contexts
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: repo-branches-all.getallstatuscheckcontexts
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

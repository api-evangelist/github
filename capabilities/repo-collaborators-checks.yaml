naftiko: 1.0.0-alpha2
info:
  label: github-repos-api — Checks
  description: 'github-repos-api — Checks. 1 operations. Lead operation: GitHub Check if User is Repository Collaborator.
    Self-contained Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Checks
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-collaborators-checks
    baseUri: ''
    description: github-repos-api — Checks business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-collaborators-username
      path: /repos/{owner}/{repo}/collaborators/{username}
      operations:
      - name: checkifuserisrepositorycollaborator
        method: GET
        description: GitHub Check if User is Repository Collaborator
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-collaborators-checks-rest
    port: 8080
    description: REST adapter for github-repos-api — Checks. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/collaborators/{username}
      name: repos-owner-repo-collaborators-username
      description: REST surface for repos-owner-repo-collaborators-username.
      operations:
      - method: GET
        name: checkifuserisrepositorycollaborator
        description: GitHub Check if User is Repository Collaborator
        call: repo-collaborators-checks.checkifuserisrepositorycollaborator
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-collaborators-checks-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repos-api — Checks. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: github-check-if-user-is
      description: GitHub Check if User is Repository Collaborator
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: repo-collaborators-checks.checkifuserisrepositorycollaborator
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

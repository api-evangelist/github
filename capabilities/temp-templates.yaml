naftiko: 1.0.0-alpha2
info:
  label: GitHub Repos API — Templates
  description: 'GitHub Repos API — Templates. 3 operations. Lead operation: Get The Customization Template For An Oidc Subject
    Claim For Repository. Self-contained Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Templates
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: temp-templates
    baseUri: ''
    description: GitHub Repos API — Templates business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-actions-oidc-customization-sub
      path: /repos/{owner}/{repo}/actions/oidc/customization/sub
      operations:
      - name: getthecustomizationtemplateforanoidcsubjectclaimforrepository
        method: GET
        description: Get The Customization Template For An Oidc Subject Claim For Repository
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
      - name: setthecustomizationtemplateforanoidcsubjectclaimforrepository
        method: PUT
        description: Set The Customization Template For An Oidc Subject Claim For Repository
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: repos-template_owner-template_repo-generate
      path: /repos/{template_owner}/{template_repo}/generate
      operations:
      - name: createrepositoryusingtemplate
        method: POST
        description: Create Repository Using Template
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: template_owner
          in: path
          type: string
          description: The account owner of the template repository. The name is not case sensitive.
          required: true
        - name: template_repo
          in: path
          type: string
          description: The name of the template repository without the `.git` extension. The name is not case sensitive.
          required: true
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: temp-templates-rest
    port: 8080
    description: REST adapter for GitHub Repos API — Templates. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/actions/oidc/customization/sub
      name: repos-owner-repo-actions-oidc-customization-sub
      description: REST surface for repos-owner-repo-actions-oidc-customization-sub.
      operations:
      - method: GET
        name: getthecustomizationtemplateforanoidcsubjectclaimforrepository
        description: Get The Customization Template For An Oidc Subject Claim For Repository
        call: temp-templates.getthecustomizationtemplateforanoidcsubjectclaimforrepository
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: setthecustomizationtemplateforanoidcsubjectclaimforrepository
        description: Set The Customization Template For An Oidc Subject Claim For Repository
        call: temp-templates.setthecustomizationtemplateforanoidcsubjectclaimforrepository
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/repos/{template-owner}/{template-repo}/generate
      name: repos-template-owner-template-repo-generate
      description: REST surface for repos-template_owner-template_repo-generate.
      operations:
      - method: POST
        name: createrepositoryusingtemplate
        description: Create Repository Using Template
        call: temp-templates.createrepositoryusingtemplate
        with:
          template_owner: rest.template_owner
          template_repo: rest.template_repo
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: temp-templates-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitHub Repos API — Templates. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: get-customization-template-oidc-subject
      description: Get The Customization Template For An Oidc Subject Claim For Repository
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: temp-templates.getthecustomizationtemplateforanoidcsubjectclaimforrepository
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.
    - name: set-customization-template-oidc-subject
      description: Set The Customization Template For An Oidc Subject Claim For Repository
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: temp-templates.setthecustomizationtemplateforanoidcsubjectclaimforrepository
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: create-repository-using-template
      description: Create Repository Using Template
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: temp-templates.createrepositoryusingtemplate
      with:
        template_owner: tools.template_owner
        template_repo: tools.template_repo
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.

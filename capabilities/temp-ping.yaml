naftiko: 1.0.0-alpha2
info:
  label: GitHub Repos API — Ping
  description: 'GitHub Repos API — Ping. 1 operations. Lead operation: Ping Repository Webhook. Self-contained Naftiko capability
    covering one Github business surface.'
  tags:
  - Github
  - Ping
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: temp-ping
    baseUri: ''
    description: GitHub Repos API — Ping business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-hooks-hook_id-pings
      path: /repos/{owner}/{repo}/hooks/{hook_id}/pings
      operations:
      - name: pingrepositorywebhook
        method: POST
        description: Ping Repository Webhook
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: temp-ping-rest
    port: 8080
    description: REST adapter for GitHub Repos API — Ping. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/hooks/{hook-id}/pings
      name: repos-owner-repo-hooks-hook-id-pings
      description: REST surface for repos-owner-repo-hooks-hook_id-pings.
      operations:
      - method: POST
        name: pingrepositorywebhook
        description: Ping Repository Webhook
        call: temp-ping.pingrepositorywebhook
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: temp-ping-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitHub Repos API — Ping. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: ping-repository-webhook
      description: Ping Repository Webhook
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: temp-ping.pingrepositorywebhook
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

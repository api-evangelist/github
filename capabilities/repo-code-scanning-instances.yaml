naftiko: 1.0.0-alpha2
info:
  label: github-repos-api — Instances
  description: 'github-repos-api — Instances. 1 operations. Lead operation: GitHub List Instances of Code Scanning Alert.
    Self-contained Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Instances
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-code-scanning-instances
    baseUri: ''
    description: github-repos-api — Instances business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-code-scanning-alerts-alert_number-instances
      path: /repos/{owner}/{repo}/code-scanning/alerts/{alert_number}/instances
      operations:
      - name: listinstancesofcodescanningalert
        method: GET
        description: GitHub List Instances of Code Scanning Alert
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-code-scanning-instances-rest
    port: 8080
    description: REST adapter for github-repos-api — Instances. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/code-scanning/alerts/{alert-number}/instances
      name: repos-owner-repo-code-scanning-alerts-alert-number-instances
      description: REST surface for repos-owner-repo-code-scanning-alerts-alert_number-instances.
      operations:
      - method: GET
        name: listinstancesofcodescanningalert
        description: GitHub List Instances of Code Scanning Alert
        call: repo-code-scanning-instances.listinstancesofcodescanningalert
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-code-scanning-instances-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repos-api — Instances. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: github-list-instances-code-scanning
      description: GitHub List Instances of Code Scanning Alert
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: repo-code-scanning-instances.listinstancesofcodescanningalert
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

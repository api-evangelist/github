naftiko: 1.0.0-alpha2
info:
  label: GitHub Repos API — Directories
  description: 'GitHub Repos API — Directories. 1 operations. Lead operation: Get Repository Readme For Directory. Self-contained
    Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Directories
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: temp-directories
    baseUri: ''
    description: GitHub Repos API — Directories business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-readme-dir
      path: /repos/{owner}/{repo}/readme/{dir}
      operations:
      - name: getrepositoryreadmefordirectory
        method: GET
        description: Get Repository Readme For Directory
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: dir
          in: path
          type: string
          description: The alternate path to look for a README file
          required: true
        - name: ref
          in: query
          type: string
          description: 'The name of the commit/branch/tag. Default: the repositorys default branch.'
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: temp-directories-rest
    port: 8080
    description: REST adapter for GitHub Repos API — Directories. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/readme/{dir}
      name: repos-owner-repo-readme-dir
      description: REST surface for repos-owner-repo-readme-dir.
      operations:
      - method: GET
        name: getrepositoryreadmefordirectory
        description: Get Repository Readme For Directory
        call: temp-directories.getrepositoryreadmefordirectory
        with:
          dir: rest.dir
          ref: rest.ref
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: temp-directories-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitHub Repos API — Directories. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: get-repository-readme-directory
      description: Get Repository Readme For Directory
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: temp-directories.getrepositoryreadmefordirectory
      with:
        dir: tools.dir
        ref: tools.ref
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

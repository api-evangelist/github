naftiko: 1.0.0-alpha2
info:
  label: github-repos-api — Contributor
  description: 'github-repos-api — Contributor. 1 operations. Lead operation: GitHub Get All Contributor Commit Activity.
    Self-contained Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Contributor
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-tags-contributor
    baseUri: ''
    description: github-repos-api — Contributor business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-stats-contributors
      path: /repos/{owner}/{repo}/stats/contributors
      operations:
      - name: getallcontributorcommitactivity
        method: GET
        description: GitHub Get All Contributor Commit Activity
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-tags-contributor-rest
    port: 8080
    description: REST adapter for github-repos-api — Contributor. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/stats/contributors
      name: repos-owner-repo-stats-contributors
      description: REST surface for repos-owner-repo-stats-contributors.
      operations:
      - method: GET
        name: getallcontributorcommitactivity
        description: GitHub Get All Contributor Commit Activity
        call: repo-tags-contributor.getallcontributorcommitactivity
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-tags-contributor-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repos-api — Contributor. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: github-get-all-contributor-commit
      description: GitHub Get All Contributor Commit Activity
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: repo-tags-contributor.getallcontributorcommitactivity
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

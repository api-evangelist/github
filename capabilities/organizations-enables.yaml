naftiko: 1.0.0-alpha2
info:
  label: GitHub Org API — Enables
  description: 'GitHub Org API — Enables. 2 operations. Lead operation: GitHub Enable Selected Repository for Github Actions
    in an Organization. Self-contained Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Enables
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: organizations-enables
    baseUri: ''
    description: GitHub Org API — Enables business capability. Self-contained, no shared references.
    resources:
    - name: orgs-org-actions-permissions-repositories-repository_id
      path: /orgs/{org}/actions/permissions/repositories/{repository_id}
      operations:
      - name: enableselectedrepositoryforgithubactionsinanorganization
        method: PUT
        description: GitHub Enable Selected Repository for Github Actions in an Organization
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    - name: orgs-org-security_product-enablement
      path: /orgs/{org}/{security_product}/{enablement}
      operations:
      - name: enableordisablesecurityfeatureforanorganization
        method: POST
        description: GitHub Enable or Disable Security Feature for an Organization
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: organizations-enables-rest
    port: 8080
    description: REST adapter for GitHub Org API — Enables. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/orgs/{org}/actions/permissions/repositories/{repository-id}
      name: orgs-org-actions-permissions-repositories-repository-id
      description: REST surface for orgs-org-actions-permissions-repositories-repository_id.
      operations:
      - method: PUT
        name: enableselectedrepositoryforgithubactionsinanorganization
        description: GitHub Enable Selected Repository for Github Actions in an Organization
        call: organizations-enables.enableselectedrepositoryforgithubactionsinanorganization
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/orgs/{org}/{security-product}/{enablement}
      name: orgs-org-security-product-enablement
      description: REST surface for orgs-org-security_product-enablement.
      operations:
      - method: POST
        name: enableordisablesecurityfeatureforanorganization
        description: GitHub Enable or Disable Security Feature for an Organization
        call: organizations-enables.enableordisablesecurityfeatureforanorganization
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: organizations-enables-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitHub Org API — Enables. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: github-enable-selected-repository-github
      description: GitHub Enable Selected Repository for Github Actions in an Organization
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: organizations-enables.enableselectedrepositoryforgithubactionsinanorganization
      outputParameters:
      - type: object
        mapping: $.
    - name: github-enable-disable-security-feature
      description: GitHub Enable or Disable Security Feature for an Organization
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: organizations-enables.enableordisablesecurityfeatureforanorganization
      outputParameters:
      - type: object
        mapping: $.

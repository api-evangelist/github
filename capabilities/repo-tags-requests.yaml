naftiko: 1.0.0-alpha2
info:
  label: github-repos-api — Requests
  description: 'github-repos-api — Requests. 2 operations. Lead operation: GitHub List Pull Requests Associated with Commit.
    Self-contained Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Requests
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-tags-requests
    baseUri: ''
    description: github-repos-api — Requests business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-commits-commit_sha-pulls
      path: /repos/{owner}/{repo}/commits/{commit_sha}/pulls
      operations:
      - name: listpullrequestsassociatedwithcommit
        method: GET
        description: GitHub List Pull Requests Associated with Commit
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    - name: repos-owner-repo-pages-builds
      path: /repos/{owner}/{repo}/pages/builds
      operations:
      - name: requestgithubenterpriseserverpagesbuild
        method: POST
        description: GitHub Request Github Enterprise Server Pages Build
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-tags-requests-rest
    port: 8080
    description: REST adapter for github-repos-api — Requests. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/commits/{commit-sha}/pulls
      name: repos-owner-repo-commits-commit-sha-pulls
      description: REST surface for repos-owner-repo-commits-commit_sha-pulls.
      operations:
      - method: GET
        name: listpullrequestsassociatedwithcommit
        description: GitHub List Pull Requests Associated with Commit
        call: repo-tags-requests.listpullrequestsassociatedwithcommit
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/repos/{owner}/{repo}/pages/builds
      name: repos-owner-repo-pages-builds
      description: REST surface for repos-owner-repo-pages-builds.
      operations:
      - method: POST
        name: requestgithubenterpriseserverpagesbuild
        description: GitHub Request Github Enterprise Server Pages Build
        call: repo-tags-requests.requestgithubenterpriseserverpagesbuild
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-tags-requests-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repos-api — Requests. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: github-list-pull-requests-associated
      description: GitHub List Pull Requests Associated with Commit
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: repo-tags-requests.listpullrequestsassociatedwithcommit
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.
    - name: github-request-github-enterprise-server
      description: GitHub Request Github Enterprise Server Pages Build
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: repo-tags-requests.requestgithubenterpriseserverpagesbuild
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

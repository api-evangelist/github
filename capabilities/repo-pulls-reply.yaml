naftiko: 1.0.0-alpha2
info:
  label: github-repos-api — Reply
  description: 'github-repos-api — Reply. 1 operations. Lead operation: GitHub Create Reply for Review Comment. Self-contained
    Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Reply
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-pulls-reply
    baseUri: ''
    description: github-repos-api — Reply business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-pulls-pull_number-comments-comment_id-replies
      path: /repos/{owner}/{repo}/pulls/{pull_number}/comments/{comment_id}/replies
      operations:
      - name: createreplyforreviewcomment
        method: POST
        description: GitHub Create Reply for Review Comment
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-pulls-reply-rest
    port: 8080
    description: REST adapter for github-repos-api — Reply. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/pulls/{pull-number}/comments/{comment-id}/replies
      name: repos-owner-repo-pulls-pull-number-comments-comment-id-replies
      description: REST surface for repos-owner-repo-pulls-pull_number-comments-comment_id-replies.
      operations:
      - method: POST
        name: createreplyforreviewcomment
        description: GitHub Create Reply for Review Comment
        call: repo-pulls-reply.createreplyforreviewcomment
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-pulls-reply-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repos-api — Reply. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: github-create-reply-review-comment
      description: GitHub Create Reply for Review Comment
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: repo-pulls-reply.createreplyforreviewcomment
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.

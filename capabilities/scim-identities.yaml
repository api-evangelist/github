naftiko: 1.0.0-alpha2
info:
  label: GitHub SCIM API — Identities
  description: 'GitHub SCIM API — Identities. 1 operations. Lead operation: GitHub List Scim Provisioned Identities for an
    Enterprise. Self-contained Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Identities
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: scim-identities
    baseUri: ''
    description: GitHub SCIM API — Identities business capability. Self-contained, no shared references.
    resources:
    - name: scim-v2-Users
      path: /scim/v2/Users
      operations:
      - name: listscimprovisionedidentitiesforanenterprise
        method: GET
        description: GitHub List Scim Provisioned Identities for an Enterprise
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: filter
          in: query
          type: string
          description: 'If specified, only results that match the specified filter will be returned. Multiple filters are
            not supported. Possible filters are `userName`, `externalId`, '
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: scim-identities-rest
    port: 8080
    description: REST adapter for GitHub SCIM API — Identities. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/scim/v2/users
      name: scim-v2-users
      description: REST surface for scim-v2-Users.
      operations:
      - method: GET
        name: listscimprovisionedidentitiesforanenterprise
        description: GitHub List Scim Provisioned Identities for an Enterprise
        call: scim-identities.listscimprovisionedidentitiesforanenterprise
        with:
          filter: rest.filter
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: scim-identities-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitHub SCIM API — Identities. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: github-list-scim-provisioned-identities
      description: GitHub List Scim Provisioned Identities for an Enterprise
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: scim-identities.listscimprovisionedidentitiesforanenterprise
      with:
        filter: tools.filter
      outputParameters:
      - type: object
        mapping: $.

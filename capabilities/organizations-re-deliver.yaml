naftiko: 1.0.0-alpha2
info:
  label: GitHub Org API — Re-Deliver
  description: 'GitHub Org API — Re-Deliver. 1 operations. Lead operation: GitHub Redeliver Delivery for an Organization Webhook.
    Self-contained Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Re-Deliver
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: organizations-re-deliver
    baseUri: ''
    description: GitHub Org API — Re-Deliver business capability. Self-contained, no shared references.
    resources:
    - name: orgs-org-hooks-hook_id-deliveries-delivery_id-attempts
      path: /orgs/{org}/hooks/{hook_id}/deliveries/{delivery_id}/attempts
      operations:
      - name: redeliverdeliveryforanorganizationwebhook
        method: POST
        description: GitHub Redeliver Delivery for an Organization Webhook
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: organizations-re-deliver-rest
    port: 8080
    description: REST adapter for GitHub Org API — Re-Deliver. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/orgs/{org}/hooks/{hook-id}/deliveries/{delivery-id}/attempts
      name: orgs-org-hooks-hook-id-deliveries-delivery-id-attempts
      description: REST surface for orgs-org-hooks-hook_id-deliveries-delivery_id-attempts.
      operations:
      - method: POST
        name: redeliverdeliveryforanorganizationwebhook
        description: GitHub Redeliver Delivery for an Organization Webhook
        call: organizations-re-deliver.redeliverdeliveryforanorganizationwebhook
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: organizations-re-deliver-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitHub Org API — Re-Deliver. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: github-redeliver-delivery-organization-webhook
      description: GitHub Redeliver Delivery for an Organization Webhook
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: organizations-re-deliver.redeliverdeliveryforanorganizationwebhook
      outputParameters:
      - type: object
        mapping: $.

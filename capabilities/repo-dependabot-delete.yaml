naftiko: 1.0.0-alpha2
info:
  label: github-repos-api — Delete
  description: 'github-repos-api — Delete. 1 operations. Lead operation: GitHub Delete Repository Secret. Self-contained Naftiko
    capability covering one Github business surface.'
  tags:
  - Github
  - Delete
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-dependabot-delete
    baseUri: ''
    description: github-repos-api — Delete business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-dependabot-secrets-secret_name
      path: /repos/{owner}/{repo}/dependabot/secrets/{secret_name}
      operations:
      - name: deleterepositorydependabotsecret
        method: DELETE
        description: GitHub Delete Repository Secret
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-dependabot-delete-rest
    port: 8080
    description: REST adapter for github-repos-api — Delete. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/dependabot/secrets/{secret-name}
      name: repos-owner-repo-dependabot-secrets-secret-name
      description: REST surface for repos-owner-repo-dependabot-secrets-secret_name.
      operations:
      - method: DELETE
        name: deleterepositorydependabotsecret
        description: GitHub Delete Repository Secret
        call: repo-dependabot-delete.deleterepositorydependabotsecret
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-dependabot-delete-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repos-api — Delete. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: github-delete-repository-secret
      description: GitHub Delete Repository Secret
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: repo-dependabot-delete.deleterepositorydependabotsecret
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

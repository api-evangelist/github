naftiko: 1.0.0-alpha2
info:
  label: GitHub Repos API — Locks
  description: 'GitHub Repos API — Locks. 1 operations. Lead operation: Lock An Issue. Self-contained Naftiko capability covering
    one Github business surface.'
  tags:
  - Github
  - Locks
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: temp-locks
    baseUri: ''
    description: GitHub Repos API — Locks business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-issues-issue_number-lock
      path: /repos/{owner}/{repo}/issues/{issue_number}/lock
      operations:
      - name: lockanissue
        method: PUT
        description: Lock An Issue
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: temp-locks-rest
    port: 8080
    description: REST adapter for GitHub Repos API — Locks. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/issues/{issue-number}/lock
      name: repos-owner-repo-issues-issue-number-lock
      description: REST surface for repos-owner-repo-issues-issue_number-lock.
      operations:
      - method: PUT
        name: lockanissue
        description: Lock An Issue
        call: temp-locks.lockanissue
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: temp-locks-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitHub Repos API — Locks. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: lock-issue
      description: Lock An Issue
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: temp-locks.lockanissue
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.

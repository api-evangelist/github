naftiko: 1.0.0-alpha2
info:
  label: GitHub Repos API — Invitations
  description: 'GitHub Repos API — Invitations. 2 operations. Lead operation: List Repository Invitations. Self-contained
    Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Invitations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: temp-invitations
    baseUri: ''
    description: GitHub Repos API — Invitations business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-invitations
      path: /repos/{owner}/{repo}/invitations
      operations:
      - name: listrepositoryinvitations
        method: GET
        description: List Repository Invitations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    - name: user-repository_invitations
      path: /user/repository_invitations
      operations:
      - name: listrepositoryinvitationsfortheauthenticateduser
        method: GET
        description: List Repository Invitations For The Authenticated User
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: temp-invitations-rest
    port: 8080
    description: REST adapter for GitHub Repos API — Invitations. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/invitations
      name: repos-owner-repo-invitations
      description: REST surface for repos-owner-repo-invitations.
      operations:
      - method: GET
        name: listrepositoryinvitations
        description: List Repository Invitations
        call: temp-invitations.listrepositoryinvitations
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/user/repository-invitations
      name: user-repository-invitations
      description: REST surface for user-repository_invitations.
      operations:
      - method: GET
        name: listrepositoryinvitationsfortheauthenticateduser
        description: List Repository Invitations For The Authenticated User
        call: temp-invitations.listrepositoryinvitationsfortheauthenticateduser
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: temp-invitations-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitHub Repos API — Invitations. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: list-repository-invitations
      description: List Repository Invitations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: temp-invitations.listrepositoryinvitations
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.
    - name: list-repository-invitations-authenticated-user
      description: List Repository Invitations For The Authenticated User
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: temp-invitations.listrepositoryinvitationsfortheauthenticateduser
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

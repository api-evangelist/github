naftiko: 1.0.0-alpha2
info:
  label: GitHub User API — Owned
  description: 'GitHub User API — Owned. 2 operations. Lead operation: GitHub List Package Versions for Package Owned by the
    Authenticated User. Self-contained Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Owned
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: users-owned
    baseUri: ''
    description: GitHub User API — Owned business capability. Self-contained, no shared references.
    resources:
    - name: user-packages-package_type-package_name-versions
      path: /user/packages/{package_type}/{package_name}/versions
      operations:
      - name: listpackageversionsforpackageownedbytheauthenticateduser
        method: GET
        description: GitHub List Package Versions for Package Owned by the Authenticated User
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: state
          in: query
          type: string
          description: The state of the package, either active or deleted.
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    - name: users-username-packages-package_type-package_name-versions
      path: /users/{username}/packages/{package_type}/{package_name}/versions
      operations:
      - name: listpackageversionsforpackageownedbyuser
        method: GET
        description: GitHub List Package Versions for Package Owned by User
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: users-owned-rest
    port: 8080
    description: REST adapter for GitHub User API — Owned. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/user/packages/{package-type}/{package-name}/versions
      name: user-packages-package-type-package-name-versions
      description: REST surface for user-packages-package_type-package_name-versions.
      operations:
      - method: GET
        name: listpackageversionsforpackageownedbytheauthenticateduser
        description: GitHub List Package Versions for Package Owned by the Authenticated User
        call: users-owned.listpackageversionsforpackageownedbytheauthenticateduser
        with:
          state: rest.state
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/users/{username}/packages/{package-type}/{package-name}/versions
      name: users-username-packages-package-type-package-name-versions
      description: REST surface for users-username-packages-package_type-package_name-versions.
      operations:
      - method: GET
        name: listpackageversionsforpackageownedbyuser
        description: GitHub List Package Versions for Package Owned by User
        call: users-owned.listpackageversionsforpackageownedbyuser
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: users-owned-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitHub User API — Owned. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: github-list-package-versions-package
      description: GitHub List Package Versions for Package Owned by the Authenticated User
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: users-owned.listpackageversionsforpackageownedbytheauthenticateduser
      with:
        state: tools.state
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.
    - name: github-list-package-versions-package-2
      description: GitHub List Package Versions for Package Owned by User
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: users-owned.listpackageversionsforpackageownedbyuser
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

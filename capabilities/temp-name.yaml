naftiko: 1.0.0-alpha2
info:
  label: GitHub Repos API — Name
  description: 'GitHub Repos API — Name. 1 operations. Lead operation: Get Release By Tag Name. Self-contained Naftiko capability
    covering one Github business surface.'
  tags:
  - Github
  - Name
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: temp-name
    baseUri: ''
    description: GitHub Repos API — Name business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-releases-tags-tag
      path: /repos/{owner}/{repo}/releases/tags/{tag}
      operations:
      - name: getreleasebytagname
        method: GET
        description: Get Release By Tag Name
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: tag
          in: path
          type: string
          description: tag parameter
          required: true
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: temp-name-rest
    port: 8080
    description: REST adapter for GitHub Repos API — Name. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/releases/tags/{tag}
      name: repos-owner-repo-releases-tags-tag
      description: REST surface for repos-owner-repo-releases-tags-tag.
      operations:
      - method: GET
        name: getreleasebytagname
        description: Get Release By Tag Name
        call: temp-name.getreleasebytagname
        with:
          tag: rest.tag
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: temp-name-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitHub Repos API — Name. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: get-release-tag-name
      description: Get Release By Tag Name
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: temp-name.getreleasebytagname
      with:
        tag: tools.tag
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

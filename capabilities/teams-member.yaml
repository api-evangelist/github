naftiko: 1.0.0-alpha2
info:
  label: GitHub Teams API — Member
  description: 'GitHub Teams API — Member. 3 operations. Lead operation: GitHub Get Team Member (legacy). Self-contained Naftiko
    capability covering one Github business surface.'
  tags:
  - Github
  - Member
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: teams-member
    baseUri: ''
    description: GitHub Teams API — Member business capability. Self-contained, no shared references.
    resources:
    - name: teams-team_id-members-username
      path: /teams/{team_id}/members/{username}
      operations:
      - name: getteammemberlegacy
        method: GET
        description: GitHub Get Team Member (legacy)
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: addteammemberlegacy
        method: PUT
        description: GitHub Add Team Member (legacy)
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: removeteammemberlegacy
        method: DELETE
        description: GitHub Remove Team Member (legacy)
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: teams-member-rest
    port: 8080
    description: REST adapter for GitHub Teams API — Member. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/teams/{team-id}/members/{username}
      name: teams-team-id-members-username
      description: REST surface for teams-team_id-members-username.
      operations:
      - method: GET
        name: getteammemberlegacy
        description: GitHub Get Team Member (legacy)
        call: teams-member.getteammemberlegacy
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: addteammemberlegacy
        description: GitHub Add Team Member (legacy)
        call: teams-member.addteammemberlegacy
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: removeteammemberlegacy
        description: GitHub Remove Team Member (legacy)
        call: teams-member.removeteammemberlegacy
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: teams-member-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitHub Teams API — Member. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: github-get-team-member-legacy
      description: GitHub Get Team Member (legacy)
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: teams-member.getteammemberlegacy
      outputParameters:
      - type: object
        mapping: $.
    - name: github-add-team-member-legacy
      description: GitHub Add Team Member (legacy)
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: teams-member.addteammemberlegacy
      outputParameters:
      - type: object
        mapping: $.
    - name: github-remove-team-member-legacy
      description: GitHub Remove Team Member (legacy)
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: teams-member.removeteammemberlegacy
      outputParameters:
      - type: object
        mapping: $.

naftiko: 1.0.0-alpha2
info:
  label: GitHub User API — Demote
  description: 'GitHub User API — Demote. 1 operations. Lead operation: GitHub Demote Site Administrator. Self-contained Naftiko
    capability covering one Github business surface.'
  tags:
  - Github
  - Demote
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: users-demote
    baseUri: ''
    description: GitHub User API — Demote business capability. Self-contained, no shared references.
    resources:
    - name: users-username-site_admin
      path: /users/{username}/site_admin
      operations:
      - name: demotesiteadministrator
        method: DELETE
        description: GitHub Demote Site Administrator
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: users-demote-rest
    port: 8080
    description: REST adapter for GitHub User API — Demote. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/users/{username}/site-admin
      name: users-username-site-admin
      description: REST surface for users-username-site_admin.
      operations:
      - method: DELETE
        name: demotesiteadministrator
        description: GitHub Demote Site Administrator
        call: users-demote.demotesiteadministrator
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: users-demote-mcp
    port: 9090
    transport: http
    description: MCP adapter for GitHub User API — Demote. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: github-demote-site-administrator
      description: GitHub Demote Site Administrator
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: users-demote.demotesiteadministrator
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.

naftiko: 1.0.0-alpha2
info:
  label: github-repos-api — Notifications
  description: 'github-repos-api — Notifications. 2 operations. Lead operation: GitHub List Repository Notifications for the
    Authenticated User. Self-contained Naftiko capability covering one Github business surface.'
  tags:
  - Github
  - Notifications
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    GITHUB_API_KEY: GITHUB_API_KEY
capability:
  consumes:
  - type: http
    namespace: repo-tags-notifications
    baseUri: ''
    description: github-repos-api — Notifications business capability. Self-contained, no shared references.
    resources:
    - name: repos-owner-repo-notifications
      path: /repos/{owner}/{repo}/notifications
      operations:
      - name: listrepositorynotificationsfortheauthenticateduser
        method: GET
        description: GitHub List Repository Notifications for the Authenticated User
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
      - name: markrepositorynotificationsasread
        method: PUT
        description: GitHub Mark Repository Notifications as Read
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: Authorization
          in: header
          type: string
        - name: X-GitHub-Api-Version
          in: header
          type: string
        - name: Accept
          in: header
          type: string
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: bearer
      token: '{{env.GITHUB_API_KEY}}'
  exposes:
  - type: rest
    namespace: repo-tags-notifications-rest
    port: 8080
    description: REST adapter for github-repos-api — Notifications. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/repos/{owner}/{repo}/notifications
      name: repos-owner-repo-notifications
      description: REST surface for repos-owner-repo-notifications.
      operations:
      - method: GET
        name: listrepositorynotificationsfortheauthenticateduser
        description: GitHub List Repository Notifications for the Authenticated User
        call: repo-tags-notifications.listrepositorynotificationsfortheauthenticateduser
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
        outputParameters:
        - type: object
          mapping: $.
      - method: PUT
        name: markrepositorynotificationsasread
        description: GitHub Mark Repository Notifications as Read
        call: repo-tags-notifications.markrepositorynotificationsasread
        with:
          Authorization: rest.Authorization
          X-GitHub-Api-Version: rest.X-GitHub-Api-Version
          Accept: rest.Accept
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: repo-tags-notifications-mcp
    port: 9090
    transport: http
    description: MCP adapter for github-repos-api — Notifications. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: github-list-repository-notifications-authenticated
      description: GitHub List Repository Notifications for the Authenticated User
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: repo-tags-notifications.listrepositorynotificationsfortheauthenticateduser
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
      outputParameters:
      - type: object
        mapping: $.
    - name: github-mark-repository-notifications-read
      description: GitHub Mark Repository Notifications as Read
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: repo-tags-notifications.markrepositorynotificationsasread
      with:
        Authorization: tools.Authorization
        X-GitHub-Api-Version: tools.X-GitHub-Api-Version
        Accept: tools.Accept
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
